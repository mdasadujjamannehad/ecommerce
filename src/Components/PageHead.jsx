import React from 'react'

const PageHead = ({PageHead,className}) => {
  return (
    <h1 className={`${className}  font-bold font-dmFonts text-black `} >{PageHead}</h1>
  )
}

export default PageHead